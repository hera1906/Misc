== Merge ==

git merge --no-ff origin/master         # Merge master into current branch
git push
git mergetool                           # Solve conflicts

git checkout master                     # Switch to master git pull
git merge --no-ff <branchname>          # merge branch into master
